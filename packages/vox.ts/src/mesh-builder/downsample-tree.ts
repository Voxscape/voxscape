import { ParsedVoxFile, VoxelModel, Voxel, VoxelGroup } from '../types/vox-types';

/**
 *
 */
export function downsampleTree(m: VoxelModel, factor: number): VoxelModel {
  throw new Error(`TODO`);
}
